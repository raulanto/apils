# Local envs / caches
.venv
__pycache__
*.py[oc]
.pytest_cache
.ruff_cache
.mypy_cache

# Local data / db (serverless filesystem is read-only + ephemeral)
data
test.db
*.sqlite
*.sqlite3

# Not needed at runtime on Vercel (deps come from requirements.txt)
uv.lock

# Tooling / meta
.git
.agents
.claude
skills-lock.json
icon.svg
# NOTE: README.md is required by pyproject.toml ([project].readme) — do not ignore it,
# the `uv_build` wheel build fails without it.

# Migrations are not run in the serverless function; run them from CI/locally
alembic
alembic.ini
